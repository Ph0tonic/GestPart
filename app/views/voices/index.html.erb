<!-- Content Header (Page header) -->
<div class="content-header">
  <div class="container-fluid">
    <div class="row mb-2">
      <div class="col-sm-6">
        <h1 class="m-0 text-dark">Voices</h1>
      </div><!-- /.col -->
      <div class="col-sm-6">
        <ol class="breadcrumb float-sm-right">
          <li class="breadcrumb-item active">Voices</li>
        </ol>
      </div><!-- /.col -->
    </div><!-- /.row -->
  </div><!-- /.container-fluid -->
</div>
<!-- /.content-header -->

<!-- Main content -->
<div class="content">
  <div class="container-fluid">
    <div class="row">
      <div class="col-lg-12">
        <div class="card">
          <div class="card-header">
            <div class="row">
              <div class="col-sm-6">
                <h3 class="card-title">Voices</h3>
              </div>
              <div class="col-sm-6">
                <%= link_to(new_voice_path, class: "btn btn-success float-right") do %>
                  <i class="fas fa-plus-circle"></i> <span>Add New Voice</span>
                <% end %>
              </div>
            </div>
          </div>
          <!-- /.card-header -->
          <div class="card-body">
            <table id="voices-table" class="table table-bordered table-striped dt-responsive nowrap dataTable" role="grid" aria-describedby="pieces-info" style="width:100%">
              <thead>
                <tr role="row">
                  <th class="sorting" tabindex="0" aria-controls="voices-table" rowspan="1" colspan="1" aria-label="Voice">Voice</th>
                  <th class="sorting" tabindex="0" aria-controls="voices-table" rowspan="1" colspan="1" aria-label="Number">Number</th>
                  <th class="sorting" tabindex="0" aria-controls="voices-table" rowspan="1" colspan="1" aria-label="Number">Key</th>
                  <th class="sorting_asc" tabindex="0" aria-controls="voices-table" rowspan="1" colspan="1" aria-sort="ascending" aria-label="Order">Order</th>
                  <th>Actions</th>
                </tr>
              </thead>
              <tbody>
                <% @voices.each do |voice| %>
                <tr role="row" class="odd">
                  <td class="sorting_1"><%= voice.name %></td>
                  <td><%= voice.number %></td>
                  <td><%= voice.key %></td>
                  <td><%= voice.order %></td>
                  <td>
                    <ul class="list-inline">
                      <li class="list-inline-item">
                        <%= link_to(voice) do %>
                          <i class="far fa-eye"></i>
                        <% end %>
                      </li>
                      <li class="list-inline-item">
                        <%= link_to(edit_voice_path(voice)) do %>
                          <i class="fas fa-pencil-alt text-warning"></i>
                        <% end %>
                      </li>
                      <li class="list-inline-item">
                        <%= link_to(voice, method: :delete, data: { confirm: 'Are you sure?' }) do %>
                          <i class="fas fa-trash text-danger"></i>
                        <% end %>
                      </li>
                    </ul>
                  </td>
                </tr>
                <% end %>
              </tbody>
              <tfoot>
                <tr>
                  <th class="sorting" tabindex="0" aria-controls="voices-table" rowspan="1" colspan="1" aria-label="Voice">Voice</th>
                  <th class="sorting" tabindex="0" aria-controls="voices-table" rowspan="1" colspan="1" aria-label="Number">Number</th>
                  <th class="sorting" tabindex="0" aria-controls="voices-table" rowspan="1" colspan="1" aria-label="Key">Key</th>
                  <th class="sorting_asc" tabindex="0" aria-controls="voices-table" rowspan="1" colspan="1" aria-sort="ascending" aria-label="Order">Order</th>
                  <th>Actions</th>
                </tr>
              </tfoot>
            </table>
            <script>
            $(document).ready( function () {
              $('#voices-table').DataTable({
                "order": [[ 3, "asc" ]],
                "columns": [
                  null,
                  null,
                  null,
                  null,
                  { "orderable": false, "searchable": false}
                ]});
            });
            </script>
          </div>
        </div>
        <!-- /.card-body -->
      </div>
    </div>
    <!-- /.row -->
  </div><!-- /.container-fluid -->
</div>
<!-- /.content -->


<p id="notice"><%= notice %></p>
